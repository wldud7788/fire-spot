import React from "react";
import { Meet } from "../../types/meet.types";
type Props = {
  meetDetail: Meet;
};
const MeetContentSection = ({ meetDetail }: Props) => {
  return <div>MeetContentSection</div>;
};

export default MeetContentSection;
